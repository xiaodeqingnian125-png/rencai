.DS_Store

# WeChat DevTools local/private settings
project.private.config.json

# Dependencies and generated packages
node_modules/
miniprogram/miniprogram_npm/
cloudfunctions/**/node_modules/

# Build outputs and logs generated by tools
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local environment files
.env
.env.*

# Cloud function local secrets (deployed with function, not committed)
cloudfunctions/rencai/env.js

# gstack (local-only install per user preference; CLAUDE.md is committed separately)
.claude/skills/
