# 2026-07-24 我的页面管理员入口精简

## 改动

- 对所有用户移除了“设置”功能条及其页面跳转分支。
- 对管理员移除了“管理员”折叠按钮、展开状态和动画容器。
- 管理员进入“我的”页面后，直接看到“内容管理”标题及原有的公寓、户型、活动、服务、物品、评论、用户管理七个入口。
- 管理员权限校验、入口顺序、跳转页面和数据库均未修改；非管理员仍不会看到内容管理区域。

## 验证

- 新增 `tests/profile-admin-direct-navigation.test.js`，覆盖设置入口移除、折叠层移除、管理员内容管理区直接渲染和七个入口保留。
- `node --test tests/profile-admin-direct-navigation.test.js`：1 通过，0 失败。
- `node --test tests/*.test.js`：65 通过，0 失败。
- 尝试通过开发者工具自动化检查模拟器元素，但该自动化通道未返回页面元素结果；未将其作为视觉验证结论。
