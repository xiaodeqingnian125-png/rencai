# 2026-07-24 公寓费用恢复与导入保护

## 已执行的数据操作

- 在恢复前备份了云端 `apartments` 集合：`outputs/db-backups/apartments-before-fee-recovery-2026-07-24.json`。
- 以 `人才公寓-20260724-182343.xlsx` 为唯一来源，按公寓编号恢复 77 套公寓的水费、电费、燃气费、物业费、暖气费、停车费六项原始单元格文本。
- 删除了经用户确认的重复 A001 文档 `_id=6d758f346a6339dc0011382a653fe0d4`；删除使用文档 ID 直接操作，没有触发关联收藏或户型级联删除。

## 根因与修复

- 根因：前端已使用新费用表头，但线上旧导入解析未同步；旧解析会把未识别的三列视为空。同时导出端会去除费用文本中的单位。
- 本地导入与导出代码现在要求六个完整的新表头，并保留费用原文。
- 受微信开发者工具增量部署只能可靠上传根目录文件、完整部署被本地目录项阻断的限制，线上 `index.js` 已部署入口保护：导入预览前校验六个表头；确认写入前以 Excel 原始单元格重新写入六项费用，防止旧解析器再清空列。

## 验证结果

- 恢复接口返回：更新 77 条，删除 1 条重复 A001。
- 云端读回：`apartments` 共 77 条，重复记录不存在，第一份 Excel 与数据库六项费用逐项比对 `0` 处差异。
- 用当前导出代码将恢复后的云数据生成表格并与第一份 Excel 比对：77 行，费用差异 `0`。
- 自动化测试：`node --test tests/*.test.js`，64 通过、0 失败。
