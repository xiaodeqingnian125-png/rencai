# 公寓导入表格样式确认版

日期：2026-07-20

## 输出

- 基于管理员导出的 `人才公寓-20260720-172444.xlsx` 制作样式确认版。
- 第一个工作表仍为 `公寓`，字段顺序和数据未改变，保持可导入。
- 新增第二个工作表 `填写说明`，说明地址、编号、状态和图片路径的填写规则。

## 样式

- 橙色表头、冻结首行、字段分组底色、可读列宽和自动换行。
- `状态` 列提供 `active` / `hidden` 下拉选项。
- 当前含地址冲突的 A007 行以浅橙色标识，便于修正后再导入。

## 已发现的数据问题

- A007 的区域是“高新区”，地址却包含“中原区沟赵乡”，行政区冲突会导致地址地理编码失败。
- 导入前应替换为腾讯地图可搜索到的准确完整地址，或同时填写经纬度。
