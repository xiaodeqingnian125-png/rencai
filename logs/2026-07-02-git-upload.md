# 2026-07-02 Git 上传

## 改动范围

- 准备将本轮静态 MVP 页面、服务/活动/借物页面流、管理员 7 个子页面视觉还原和相关日志提交到 Git。
- 本次提交包含 `miniprogram` 页面与静态数据、`app.json` 路由、`project.config.json` 项目名修正，以及本轮新增的 `logs` 记录。
- `AGENTS.md` 保持为本地未提交改动，避免把工作指令文件的非产品差异混入本次提交。

## 验证

- 已在提交前运行全量 `.js` 语法检查。
- 已运行 `git diff --check`。

## 备注

- 推送目标为 `origin/main`。
